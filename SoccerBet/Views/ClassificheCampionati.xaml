<?xml version="1.0" encoding="utf-8" ?>
<ContentView      xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
                  x:Class="SoccerBet.Views.ClassificheCampionati">


    <ContentView.Content>
        <syncfusion:SfListView x:Name="listView" ItemsSource="{Binding ListaCountry}" ItemSpacing="1" SelectionMode="None" BackgroundColor="#F0F0F0" ItemSize="100">
            <syncfusion:SfListView.ItemTemplate>
                <DataTemplate x:Name="ItemTemplate">
                    <Frame OutlineColor="#b3b3b3" Padding="2" HeightRequest="100" WidthRequest="100">
                        <Image Source="{Binding LinkImage}" HeightRequest="100" WidthRequest="100" Aspect="AspectFit"/>
                    </Frame>
                </DataTemplate>
            </syncfusion:SfListView.ItemTemplate>
        </syncfusion:SfListView>

    </ContentView.Content>
</ContentView>